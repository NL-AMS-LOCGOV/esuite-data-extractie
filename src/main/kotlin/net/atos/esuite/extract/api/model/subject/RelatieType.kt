package net.atos.esuite.extract.api.model.subject

enum class RelatieType {

    // Persoon 1 is ouder van persoon ('O')
    is_ouder_van,

    // Persoon 1 is kind van persoon ('K')
    is_kind_van,

    // Persoon 1 is partner van persoon ('P')
    is_partner_van,
}
